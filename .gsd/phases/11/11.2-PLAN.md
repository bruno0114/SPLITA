---
phase: 11
plan: 2
wave: 1
---

# Plan 11.2: Premium UI Components (Dropdowns)

## Objective
Replace all native HTML selects with a custom, high-fidelity `PremiumDropdown` component that matches the established glassmorphic aesthetic of the "Mis Finanzas" switcher.

## Context
- .gsd/SPEC.md
- uploaded_image_1768918614397.png (Visual reference)
- src/features/analytics/pages/Categories.tsx (Source of design)
- src/features/expenses/pages/ImportExpenses.tsx (Candidate for replacement)

## Tasks

<task type="auto">
  <name>Create PremiumDropdown Component</name>
  <files>
    - src/components/ui/PremiumDropdown.tsx
  </files>
  <action>
    Implement a reusable dropdown using `@radix-ui/react-select` (or custom Framer Motion if preferred for flexibility) that mimics the design in the provided image.
    - Glassmorphism: `bg-surface/90 backdrop-blur-xl border border-border`.
    - Grouping: Support headers like "CUENTA PERSONAL" and "GRUPOS".
    - Icons: Support Lucide icons next to items.
    - Transitions: Smooth scale/opacity entry.
  </action>
  <verify>Visual verification via browser screenshot.</verify>
  <done>Component is visually identical to the requested design and fully keyboard accessible.</done>
</task>

<task type="auto">
  <name>Global Select Replacement Wave 1</name>
  <files>
    - src/features/analytics/pages/Categories.tsx
    - src/features/expenses/pages/ImportExpenses.tsx
  </files>
  <action>
    Replace the native selects/custom scope switchers in these pages with the new `PremiumDropdown`.
    - Update `Categories.tsx` header switcher.
    - Update `ImportExpenses.tsx` destination selector.
  </action>
  <verify>Confirm functionality and visual consistency across both pages.</verify>
  <done>Native selects are removed and replaced with the premium component.</done>
</task>

## Success Criteria
- [ ] `PremiumDropdown` component is available and reusable.
- [ ] No native selects remain in `Categories.tsx` or `ImportExpenses.tsx`.
- [ ] Dropdown behavior is smooth and mobile-responsive.
