# Plan 15.1 Summary
- Implemented smart routing in `ProtectedRoute.tsx`: redirects to `/onboarding` if never visited, else `/login`.
- Updated `Onboarding.tsx`:
  - Sets `has_visited` in localStorage on start or login.
  - Removed "Cerrar" button.
  - Uses `min-h-[100dvh]` for mobile viewport.
- Updated `Login.tsx`:
  - Uses `min-h-[100dvh]` for mobile viewport.

Status: Verified
